#!/usr/bin/env node
import { execFile } from 'node:child_process';
import { createHash } from 'node:crypto';
import { lstat, mkdir, open, readFile, readdir, statfs } from 'node:fs/promises';
import { dirname, relative, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { promisify } from 'node:util';
import { runBudgetedProcess } from './lib/budgeted-process.mjs';

const execFileAsync=promisify(execFile), repositoryRoot=dirname(dirname(fileURLToPath(import.meta.url)));
const SPEC_URI='specs/b62-camera-quality-holdout-render-validation.v0.1.json', PROTOCOL_URI='research/2026-08-29-b62-d4-holdout-render-validation-protocol.md', ROOT_URI='experiments/b62-camera-quality-holdout-render-v0-1', PARENT_ROOT='experiments/b62-camera-quality-bounded-candidate-search-v0-2', PREREGISTRATION_COMMIT='c2941a1';
const TOOLS=['blender/build_b62_q1_d4_corrected_scene.py','blender/render_b62_q1_d4_holdout_pairs.py','blender/audit_b62_q1_d4_scene_and_pixels.py','scripts/run-b62-q1-d4-holdout-render.mjs','scripts/audit-b62-q1-d4-holdout-render.mjs'];
function canonicalize(v){if(Array.isArray(v))return v.map(canonicalize);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).sort(([a],[b])=>a.localeCompare(b)).map(([k,x])=>[k,canonicalize(x)]));return v}
const canonical=v=>JSON.stringify(canonicalize(v)), hashBytes=v=>createHash('sha256').update(v).digest('hex'), hashFile=async p=>hashBytes(await readFile(p));
function req(x,m){if(!x)throw new Error(m)}
function pathFor(uri){req(uri&&!uri.startsWith('/')&&!uri.split('/').includes('..'),`unsafe ${uri}`);const p=resolve(repositoryRoot,uri);req(!relative(repositoryRoot,p).startsWith('../'),`escaped ${uri}`);return p}
async function exists(p){try{await lstat(p);return true}catch(e){if(e.code==='ENOENT')return false;throw e}}
async function git(args,encoding='utf8'){return(await execFileAsync('/usr/bin/git',args,{cwd:repositoryRoot,encoding,timeout:15000,maxBuffer:33554432,env:{PATH:'/usr/bin:/bin',LANG:'C',LC_ALL:'C',GIT_CONFIG_NOSYSTEM:'1',GIT_TERMINAL_PROMPT:'0'}})).stdout}
async function committedHash(commit,uri){return hashBytes(await git(['show',`${commit}:${uri}`],null))}
function validSelf(v,f){if(!v||!/[0-9a-f]{64}/.test(v[f]??''))return false;const c=structuredClone(v),e=c[f];delete c[f];return hashBytes(canonical(c))===e}
async function writeHashed(p,v,f){const b=structuredClone(v);b[f]=hashBytes(canonical(b));const h=await open(p,'wx',0o600);try{await h.writeFile(`${JSON.stringify(b,null,2)}\n`);await h.sync()}finally{await h.close()}return b}
async function treeIdentity(uri){const root=pathFor(uri),files=[];async function walk(d){for(const e of await readdir(d,{withFileTypes:true})){const p=resolve(d,e.name);if(e.isDirectory())await walk(p);else if(e.isFile())files.push(p);else throw new Error(`special ${p}`)}}await walk(root);files.sort();let bytes=0,material='';for(const p of files){const c=await readFile(p);bytes+=c.length;material+=`${relative(root,p).split('\\').join('/')}\0${hashBytes(c)}\n`}return{files:files.length,bytes,treeSha256:hashBytes(Buffer.from(material))}}
function parse(){const a=process.argv.slice(2);req(a.length===2&&a[0]==='--tool-freeze-commit'&&/^[0-9a-f]{40}$/.test(a[1]),'usage');return a[1]}
async function processReceipt(root,id,command,args,result){return writeHashed(resolve(root,'processes',`${id}.json`),{schemaVersion:'bfs.b62CameraQualityProcessReceipt.v0.1',experimentId:'B62-Q1-D4',processId:id,command,args,result},'processHash')}
async function child({root,id,scene,tool,args,budget,maxFiles=256}){const command=spec.runtime.blender.executable,full=['--background','--factory-startup','--disable-autoexec',scene,'--python-exit-code','1','--python',pathFor(tool),'--',...args];const result=await runBudgetedProcess({command,args:full,cwd:repositoryRoot,env:{PATH:'/usr/bin:/bin',LANG:'C.UTF-8',LC_ALL:'C.UTF-8',OCIO:resolve(repositoryRoot,'color/ocio/cg-config-v4.0.0_aces-v2.0_ocio-v2.5.ocio')},outputRoot:root,budgets:{wallTimeMs:budget*1000,maxRssBytes:spec.processBudget.maximumPeakResidentSetSizeBytesPerBlender,maxLogBytes:spec.processBudget.maximumCombinedLogBytesPerChild,maxOutputFiles:maxFiles,maxOutputBytes:spec.processBudget.projectedWriteBytes,sampleIntervalMs:100}});const receipt=await processReceipt(root,id,command,full.map(v=>v.startsWith(repositoryRoot)?relative(repositoryRoot,v).split('\\').join('/'):v),result);req(result.outcome==='PASS'&&result.child.exitCode===0&&result.breach===null,`${id} failed`);return receipt}
let spec;
async function main(){const freeze=parse();spec=JSON.parse(await readFile(pathFor(SPEC_URI),'utf8'));req(spec.experimentId==='B62-Q1-D4'&&spec.statusBeforeToolCreation==='PREREGISTERED'&&spec.output.formalRoot===ROOT_URI,'spec');req(!await exists(pathFor(ROOT_URI)),'root exists');const head=(await git(['rev-parse','HEAD'])).trim(),origin=(await git(['rev-parse','origin/main'])).trim();req(head===freeze&&origin===freeze,'freeze');await git(['merge-base','--is-ancestor',PREREGISTRATION_COMMIT,freeze]);req(await hashFile(pathFor(SPEC_URI))===await committedHash(PREREGISTRATION_COMMIT,SPEC_URI),'spec drift');req(await hashFile(pathFor(PROTOCOL_URI))===await committedHash(PREREGISTRATION_COMMIT,PROTOCOL_URI),'protocol drift');const toolHashes={};for(const uri of TOOLS){toolHashes[uri]=await hashFile(pathFor(uri));req(toolHashes[uri]===await committedHash(freeze,uri),`tool ${uri}`)}req((await git(['status','--porcelain=v1','--',SPEC_URI,PROTOCOL_URI,PARENT_ROOT,...TOOLS])).trim()==='','scoped dirty');const parentTree=await treeIdentity(PARENT_ROOT);req(canonical(parentTree)===canonical(spec.parentEvidence.d3Retry.tree),'parent tree');const parentReceipt=JSON.parse(await readFile(pathFor(spec.parentEvidence.d3Retry.receipt.uri),'utf8'));req(await hashFile(pathFor(spec.parentEvidence.d3Retry.receipt.uri))===spec.parentEvidence.d3Retry.receipt.sha256&&parentReceipt.receiptHash===spec.parentEvidence.d3Retry.receipt.receiptHash&&parentReceipt.selectedCandidateId===spec.parentEvidence.d3Retry.selectedCandidateId,'parent receipt');const master=pathFor(spec.parentEvidence.masterScene.uri),masterSha=await hashFile(master);req(masterSha===spec.parentEvidence.masterScene.sha256&&await hashFile(spec.runtime.blender.executable)===spec.runtime.blender.sha256,'runtime identity');const fs=await statfs(repositoryRoot),available=Number(fs.bavail)*Number(fs.bsize);req(available-spec.processBudget.projectedWriteBytes>=spec.processBudget.minimumFreeReserveBytes,'disk');const root=pathFor(ROOT_URI);await mkdir(resolve(root,'processes'),{recursive:true,mode:0o700});await mkdir(resolve(root,'scene'),{mode:0o700});const admission=await writeHashed(resolve(root,'admission.json'),{schemaVersion:'bfs.b62CameraQualityAdmission.v0.1',experimentId:'B62-Q1-D4',status:'ADMITTED',preregistrationCommit:PREREGISTRATION_COMMIT,toolFreezeCommit:freeze,bindings:{spec:{uri:SPEC_URI,sha256:await hashFile(pathFor(SPEC_URI))},protocol:{uri:PROTOCOL_URI,sha256:await hashFile(pathFor(PROTOCOL_URI))},parentTree,parentReceiptSha256:await hashFile(pathFor(spec.parentEvidence.d3Retry.receipt.uri)),master:{uri:spec.parentEvidence.masterScene.uri,sha256:masterSha},blenderSha256:await hashFile(spec.runtime.blender.executable),tools:toolHashes},resources:{availableBytesBefore:available,projectedWriteBytes:spec.processBudget.projectedWriteBytes,minimumFreeReserveBytes:spec.processBudget.minimumFreeReserveBytes},operations:{blenderStartsBeforeAdmission:0,renderCalls:0,modelCalls:0,networkCalls:0,dockerProcesses:0}},'admissionHash');try{const derived=resolve(root,spec.output.derivedScene),buildReport=resolve(root,'build.json');const build=await child({root,id:'BUILD',scene:master,tool:TOOLS[0],args:['--output-scene',derived,'--report',buildReport,'--master-sha256',masterSha],budget:spec.processBudget.maximumWallSecondsBuild});const buildDoc=JSON.parse(await readFile(buildReport,'utf8'));req(validSelf(buildDoc,'reportHash')&&buildDoc.status==='PASS'&&await hashFile(derived)===buildDoc.derived.sha256,'build output');const renderReport=resolve(root,'render.json');const render=await child({root,id:'RENDER',scene:derived,tool:TOOLS[1],args:['--output-dir',resolve(root,'renders'),'--report',renderReport,'--derived-sha256',buildDoc.derived.sha256],budget:spec.processBudget.maximumWallSecondsRender});const renderDoc=JSON.parse(await readFile(renderReport,'utf8'));req(validSelf(renderDoc,'reportHash')&&renderDoc.renders.length===12,'render output');const independentReport=resolve(root,'independent.json');const independent=await child({root,id:'INDEPENDENT',scene:derived,tool:TOOLS[2],args:['--output',independentReport,'--build-report',buildReport,'--render-report',renderReport,'--derived-sha256',buildDoc.derived.sha256],budget:spec.processBudget.maximumWallSecondsIndependentAudit});const independentDoc=JSON.parse(await readFile(independentReport,'utf8'));req(validSelf(independentDoc,'reportHash')&&independentDoc.status==='PASS','independent output');const auditorArgs=[pathFor(TOOLS[4]),'--root',ROOT_URI,'--tool-freeze-commit',freeze],auditorResult=await runBudgetedProcess({command:process.execPath,args:auditorArgs,cwd:repositoryRoot,env:{PATH:'/usr/bin:/bin',LANG:'C.UTF-8',LC_ALL:'C.UTF-8'},outputRoot:root,budgets:{wallTimeMs:60000,maxRssBytes:1073741824,maxLogBytes:1048576,maxOutputFiles:256,maxOutputBytes:spec.processBudget.projectedWriteBytes,sampleIntervalMs:100}}),auditor=await processReceipt(root,'AUDITOR',process.execPath,auditorArgs.map(v=>v.startsWith(repositoryRoot)?relative(repositoryRoot,v):v),auditorResult);req(auditorResult.outcome==='PASS'&&auditorResult.child.exitCode===0,'Node audit');const auditPath=resolve(root,'audit.json'),audit=JSON.parse(await readFile(auditPath,'utf8'));req(validSelf(audit,'auditHash')&&audit.status==='PASS','audit invalid');const receipt=await writeHashed(resolve(root,'receipt.json'),{schemaVersion:'bfs.b62CameraQualityHoldoutRenderReceipt.v0.1',experimentId:'B62-Q1-D4',status:'PASS',scientificVerdict:audit.scientificVerdict,humanReviewStatus:'PENDING',admission:{sha256:await hashFile(resolve(root,'admission.json')),admissionHash:admission.admissionHash},derivedScene:{uri:`${ROOT_URI}/${spec.output.derivedScene}`,sha256:buildDoc.derived.sha256,bytes:buildDoc.derived.bytes},reports:{build:{sha256:await hashFile(buildReport),reportHash:buildDoc.reportHash},render:{sha256:await hashFile(renderReport),reportHash:renderDoc.reportHash},independent:{sha256:await hashFile(independentReport),reportHash:independentDoc.reportHash},audit:{sha256:await hashFile(auditPath),auditHash:audit.auditHash}},processes:{build:{sha256:await hashFile(resolve(root,'processes/BUILD.json')),processHash:build.processHash},render:{sha256:await hashFile(resolve(root,'processes/RENDER.json')),processHash:render.processHash},independent:{sha256:await hashFile(resolve(root,'processes/INDEPENDENT.json')),processHash:independent.processHash},auditor:{sha256:await hashFile(resolve(root,'processes/AUDITOR.json')),processHash:auditor.processHash}},operations:{runnerProcesses:1,blenderStarts:3,renderCalls:12,nodeAuditorProcesses:1,modelCalls:0,networkCalls:0,dockerProcesses:0},nonClaims:spec.nonClaims},'receiptHash');console.log(`BFS_B62_Q1_D4 PASS ${receipt.scientificVerdict} HUMAN_PENDING ${receipt.receiptHash}`)}catch(e){if(!await exists(resolve(root,'failure.json')))await writeHashed(resolve(root,'failure.json'),{schemaVersion:'bfs.b62CameraQualityFailure.v0.1',experimentId:'B62-Q1-D4',status:'INVALIDATED',scientificVerdict:null,toolFreezeCommit:freeze,reason:e.message},'failureHash');throw e}}
main().catch(e=>{console.error(e.stack??e.message);process.exitCode=1});
