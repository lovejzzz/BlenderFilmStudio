# Personal studio development results

The product is in `studio/`. It runs the unchanged validated RC5 executable with separately loaded Python modules. No clean engine build, fluid bake, new paid service, binary release or public distribution occurred.

## Accepted behavior

- Two typed procedural worlds share one compiler and material/asset library, without demo-ID dispatch in geometry code.
- Native UI: direct observation confirms stable shot labels, camera selection, visible closer edit, undo, and a separate saved project version. Background UI registration alone did not establish usability:0017's corrupted EnumProperty labels were fixed and0018 was directly retested.
- Three bounded director revisions preserve actual geometry and cached motion; stale requests reject. New coverage splits time into a second angle, and undo restores the camera inventory plus an active camera.
- Model inference: three existing-account Codex calls returned two valid proposals and one unsupported-fluid rejection, all with zero tool events and zero API-key billing. No general scene-generation claim.
- Timeline correctness: previous-shot-end anchoring and overlap rejection prevent repeated action across cuts.
- Saved-state correctness: separate-process reopen matched exact mesh/pose/document state. Later random-access tests exposed a real initial-pose reset of the animated ball across release. The corrected evaluator visits authored release boundaries;0029 passes12 exact repeated jump sweeps.0030 compares all600 frames ×9 bodies against the original bake in permuted order, with maximum position/quaternion-component differences below5e-9 from rounded reference data.
- Render recovery: completed frame hashes remain exact; interrupted partial files are retained separately and only unfinished frames regenerate. Wrong snapshots/profiles reject. Per-invocation history is retained.
- Encoding:0020 verifies144 PNG frames into a6-second H.264/AAC stereo movie with original sound and an original title overlay. Native QuickTime opens and plays it to the end. Numeric audio checks do not establish a subjective listening judgment.

## Retained failures and visual rejections

Initial kinetic staging pushed below the target centers, sliding pieces backward and failing the all-target gate. Pose diagnostics led to the larger sphere/contact-height correction; no solved response was keyed. Early tape close-up/resolve framing and kinetic final-piece clipping were visually rejected and retained. The city initially appeared inside the room; a real reusable wall opening and outside placement corrected the staging. FFmpeg lacked drawtext, so0019 retained an encoder failure; the corrected local Pillow+overlay path passed.

0024/0026/0028 actual-world checks were not waived.0028 recorded a meter-scale ball-pose reset, leading to the shared cached-frame evaluator and full comparison above. The older F0.6 app was accidentally launched once by ambiguous CUA display-name resolution and closed without a film load/render/save; subsequent native selection uses the exact RC5 app path. This extra UI start remains disclosed and counted.

## Final-production boundary

The two final films are still pending completion and direct review:432 and384 frames,1920×818,24fps,Cycles96 samples. The frozen corrected product code and saved scene inputs are in the final v0.1 contract. Render exit success is only `RENDERED_PENDING_FINAL_REVIEW`; completion of the user's goal requires the finished movies, review, editable copies and usable launch instructions.
