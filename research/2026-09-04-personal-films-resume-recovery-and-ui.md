# Resume recovery and native surface verification

Extend the active-job renderer to preserve incomplete frame pairs/partial files in unique interrupted subfolders and regenerate only that incomplete frame, while retaining strict completed-frame hash validation. Individual worker invocations now retain separate result/failure records. A matching completed delivery is a no-op. Candidate0016 repeats the bounded1+2-frame resume test with a deliberately incomplete partial first frame and requires that partial be retained. It reserves3 further preview frames (150 total of192), zero stills.

Candidate0017 launches a copied accepted tape project in the actual native UI under the same isolated runtime and resource ceilings. The UI will be directly inspected, shot selection and a reversible direction edit exercised, and then the app exited. Its startup receipt alone is not visual acceptance. No in-app browser is used.
