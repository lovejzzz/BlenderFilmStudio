# RC4 unstaged physical realism — formal preregistration

Development attempt-05 is accepted at candidate commit
`db662438edfef0a1979d8227c8b58cf8620e2b74`. Formal validation now requires a
fresh local clone, zero-network LFS checkout, one clean native arm64 build, the
same R1/save-reopen/regression/negative/render suite, fresh inspection of all
formal pixels, and an independent audit.

The formal roots, exact tool hashes, operation bounds and resource ceilings are
frozen in `specs/ai-native-studio-rc4-unstaged-realism-formal.v0.1.json`.
Product publication remains contingent on machine, fresh visual and audit
passes. No force, release, signing, notarization or binary distribution is in
scope.

Before preregistration, four exact application cache directories were removed
to restore the frozen 160 GiB build admission threshold: Spotify Browser/Cache,
Spotify Default/Cache, Chrome Default/Cache and Chrome Profile 2/Cache. No
browser profile, login, history, download, extension, research evidence or
Blender workspace was removed. These caches are not recoverable from Trash but
are automatically regenerated by their applications.
