# RC6 real-impact Bullet midpoint audit-only C5 preregistration

Date: 2026-09-02

C5 binds the complete immutable 10-file attempt-75 root (`48897` bytes,
manifest hash `6a9261f1…`) and changes only one audit representation rule:
`baseVoxelMeters` and the other Blender float32 configuration values use the
same absolute `1e-6` tolerance already accepted by C3.

The physical I09 FAIL, result/receipt bytes, process/log evidence and every
threshold remain unchanged. C5 allows zero Blender, Bullet, liquid, render,
save and network operations and writes only a corrected audit plus manifest in
fresh attempt-76. It does not authorize another trajectory or liquid bake.
