# RC1 robot capstone acceptance

RC1 is accepted and published as a bounded physical-performance result.

The formal C3 attempt-02 used one clean native arm64 build and exactly three
product starts. The retained robot's authored armature expresses intention, but
the visible spring-control compression, reversal, settling and final mechanism
pose are owned by Blender Bullet. The mechanism has zero pose keyframes after
contact. A saved-workspace reopen reproduced the measured physics transforms
within `4.9419513342696675e-9` metres.

The machine receipt hash is
`df95501d05af1f78449f617bf359bc4e42a9767d817abf713f536e6eabd216ad`.
Direct visual review is `9/9 YES` with hash
`e736ea6da7df1d7a58201b755525b4d1941673cde8af6c08270fccb43fe008ef`.
The final independent audit is `37/37 PASS` with receipt hash
`e1e7c24061c84f69d52e673b836bd3838839bf8cd95e68779a5be593a975195c`.
The accepted evidence root is
`experiments/robot-capstone/RC1-2026-09-01-attempt-02`; attempt-01 remains an
immutable harness failure.

Validated product commit
`0e84ef3b6f79521b4f21a9d12a180dfd9713aab4` was published to
`lovejzzz/film-engine/main` by ordinary fast-forward. No force push, tag,
release, binary distribution, LFS upload, signing or notarization occurred.

The reusable method was captured as the validated local Codex skill
`physical-film-direction`. It teaches semantic role binding, authored
intention versus solver-owned response, derived contact, camera direction from
contact normals, semantic focus, measured native motion blur, direct still and
clip review, specific point-cache baking, and actual-transform reopen checks.

The result is not accepted as photoreal modeling, nuanced full-body acting,
deformable contact, sound, broad autonomous filmmaking or finished-film
quality. The next preregistered lesson must be simpler and unseen: a rolling
rigid body physically opens a hinged light shutter, so the illumination change
is caused by simulated geometry rather than hand-placed outcomes or animated
light intensity.
