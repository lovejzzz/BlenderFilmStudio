# PC9 C3 E1 — evidence wording correction

The retained run-03 conclusion remains valid, but its cause sentence is overbroad. The bound uncommitted module already enabled `scene.eevee.use_raytracing` for schema v0.5. The missing capability was per-material screen/raytrace refraction on the Glass BSDF bottle shell.

Historical evidence is not edited. This versioned correction binds the exact module SHA and narrows the allowed C3 product delta to the two native material refraction flags. No fixture, physical value, threshold, camera or pose changes.
