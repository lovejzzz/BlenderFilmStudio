# Encoded soundtrack normalization C1

The retained Little Gravity final passes 13/14 independent delivery checks, but its single-pass loudnorm output measures -22.4 LUFS, below the preregistered -22 through -18 range. True peak is -9.2 dBFS. All 384 frame identities, chronological sources, dimensions and complete audio/video decoding pass. Keep this production root and first audit immutable.

Under the active standing autonomy charter, admit an encode-only correction in fresh `PF-AUDIO-C1-2026-09-04-attempt-01/<film>` roots. Measure each original WAV using FFmpeg loudnorm's JSON first pass and apply its measured parameters with linear normalization toward the unchanged -20 LUFS/-2 dBTP target. Copy the original encoded video stream without re-encoding; verify identical video packets by a stream hash. Retain all measurements, commands, source hashes and corrected receipt. No Blender, rendering, simulation, new audio composition or frame changes. Maximum two corrections (one per movie, only if its initial audio gate fails), ten minutes and 256 MiB per film, 100 GiB free reserve; this fits the existing 2 GiB delivery/review envelope.

Teach the reusable encoder the same two-pass function for future user movies. Running final worker copies remain frozen. A fresh reviewer may read the original frame/root evidence with an explicit corrected delivery directory; every original technical threshold stays unchanged. Package selection must bind corrected video-stream identity and source delivery hash. Subjective listening is unavailable in this tool session and is not inferred from loudness checks.
