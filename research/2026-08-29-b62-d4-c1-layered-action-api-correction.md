# B62-Q1-D4-C1 layered Action API correction

The first D4 formal attempt failed before scene creation or rendering. Blender 5.2 reported that `Action` has no `fcurves`; the current API exposes curves through the assigned slot's channelbag. C1 permits only that API substitution in the builder plus fresh-root bindings. Render and independent-audit tool bytes, all six holdouts, the candidate, bake rule, geometry bounds, Cycles settings, 12 render calls and budgets stay frozen.
