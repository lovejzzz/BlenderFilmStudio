# RC6 液体源间距 C1 attempt-23 结果

## 结论

`RC6-2026-09-01-source-clearance-c1-attempt-23` 保留为 `FAIL_EXECUTION`。四个 Blender/Mantaflow 单元都成功完成，且每个单元只生成第 1–7 帧的 config、data、mesh 共 21 个缓存文件；封装器随后因请求间距与 Blender 从单精度场景坐标反算的实测间距不逐字相等而 fail closed。

这不是物理失败。`clearance-35mm` 的原始、不可变结果通过冻结的全部科学阈值，但必须由一个不重新启动 Blender、不重新烘焙的版本化 C2 closure 独立绑定后才能解锁下一关。

## 耗时与主机判断

| 单元 | 7 帧墙钟时间 |
|---|---:|
| 20 mm | 86.775645 s |
| 25 mm | 95.112946 s |
| 30 mm | 113.768206 s |
| 35 mm | 95.015728 s |

同一 M2 Max 主机上，修正为精确第 1–7 帧缓存后，每个真实 Mantaflow 单元约 1.4–1.9 分钟。此前“前 10 帧约 32 分钟”与旧管线的 48 帧 11,296 秒历史量级一致；其中约 11,197 秒在流体数据/网格阶段，真正渲染约 38 秒。瓶颈是缓存范围和阶段编排，不是主机性能。

## 原始科学结果

| 请求间距 | 实测间距 | 最大源体积误差 | 最大时间漂移 | 最大杯外比例 | 最大组件数 | 原始阈值判断 |
|---:|---:|---:|---:|---:|---:|---|
| 0.020 m | 0.0200000033 m | 6.318933% | 4.977575% | 0% | 2 | FAIL：源体积 |
| 0.025 m | 0.0249999985 m | 7.115483% | 5.578573% | 0% | 2 | FAIL：源体积、漂移 |
| 0.030 m | 0.0299999937 m | 2.858113% | 4.060139% | 19.779842% | 15 | FAIL：杯外、拓扑 |
| 0.035 m | 0.0350000039 m | 4.692535% | 3.817851% | 0% | 2 | PASS candidate |

`clearance-35mm` 每帧均为一个正体积外壳和最多一个完全嵌套的负体积内壳，质心间距在冻结界限内，且零非流形边、零杯外液体。组件数为 2 表示一个有内壳的封闭液体表面，不表示两个独立液滴。

## C1 封装失败

封装器按 JSON 完全相等检查 `sourceBottomClearanceMeters`。例如 35 mm 的请求值为 `0.035`，场景内实测值为 `0.0350000039`，差值约 3.9 纳米。所有四个差值都小于 1e-8 m；C2 必须分别记录 requested、measured、absolute error，并继续保持所有物理阈值不变。

不得覆盖或修补本 evidence root。C2 只能读取 retained attempt-23 的原始 result、process、log、baked-state 和精确缓存文件，并在 fresh evidence root 中生成 closure 与独立 audit。
