# Personal Film Studio 0.1.4 — local delivery

The owner requested a workable personal AI studio and two cinematic movie demos. The local package is `output/personal-film-studio/`, with two executable launchers, Chinese instructions, editable `.blend` and typed project documents, two finished MP4s, and a hash-bound provenance manifest. The existing RC5 engine is used without a new native build, public binary release or new paid service.

## Delivered films

| Film | Media | Original production | Accepted sound |
| --- | --- | --- | --- |
| The Last Signal | 18 seconds,432 frames,1920×818,24fps | 6471.33seconds,complete and zero exit | -19.9LUFS,-6.2dBFS true peak |
| A Little Gravity | 16 seconds,384 frames,1920×818,24fps | 4498.57seconds,complete and zero exit | C4 audio-only correction,-20.0LUFS,-7.4dBFS true peak |

Both used Cycles96 samples, native depth of field and motion blur. The night film moves from a warm/cool room view into reel and meter details and returns to a mechanical stop. The daylight film establishes potential energy, cuts into contact, widens for the falling chain, and holds on the changed arrangement. All816 consecutive raw frames were directly inspected through indexed contact sheets, with additional full-resolution and encoded title/cut/fade inspection. Both complete movies pass14/14 independent technical checks. The kinetic target array settles; the ball continues its native slow return and must not be described as completely stationary.

Little Gravity's original dynamic audio normalization failed the fixed loudness gate. C1 retained a parser failure; C2 dynamic processing and C3 mismatched-meter gain also failed. C4 measures the original WAV with the same EBU meter as final acceptance and applies7.3dB constant gain. Encoded video packets remain exactly unchanged. All failures and original roots are retained. The current reusable exporter uses the same measured constant-gain function.

## Actual product use verified

Native GUI tests exercised the launched studio, real Chinese AI direction using the existing authenticated Codex CLI, proposal review/apply, saved revision reopen, switching both films, and a new independently named Chinese film. Coverage edits, undo, asynchronous cross-project rejection, interrupted rendering/resume, and the native cache random-access correction have separate retained evidence. All600 native cache frames and nine bodies were compared with original solved transforms after the frame-evaluation fix. The final movie workers used frozen0.1.0 source copies;0.1.4 adds subsequent usability and audio fixes without changing those retained copies.

This is a local personal studio for reusable starter worlds and bounded AI camera/look/edit direction. It is not arbitrary scene or actor generation, autonomous long-form filmmaking, a physical tape simulator, or a newly distributed application. Two scenes provide distinct editable starting points through the same asset/compiler/render workflow.

## Review limits

The final MP4s were fully decoded and visually checked at all raw frames plus encoded editorial positions. Earlier preview playback and real studio GUI interaction passed. Final-file QuickTime playback was attempted but blocked by macOS lock screen; the owner was asked asynchronously to unlock. That check is not claimed complete. This session cannot perceive audio input, so subjective listening is not claimed; loudness, peak, stereo format, full decoding and event timing were checked. These limits are disclosed alongside the finished local artifacts rather than withholding the usable delivery.

Completion evidence: `experiments/personal-films/PF-COMPLETION-2026-09-04-attempt-01/result.json`. RC6 remains paused unchanged; no attempt118 resume, new fluid/mesh work, liquid render, native build or solver claim occurred. The native player can be checked after unlock without rerendering either movie.
