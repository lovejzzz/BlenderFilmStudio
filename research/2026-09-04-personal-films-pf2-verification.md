# PF2 workflow verification preregistration

Fresh development candidates may copy an accepted development blend into their own input root, exercise three director changes, reject stale revisions, undo and compare actual mesh coordinates plus evaluated poses at six declared frames. Save to a fresh output and reopen in a separate admitted process. Register/unregister the actual native UI to catch runtime API errors. No retained cache or source is mutated.

Add a render-plan module binding exact blend bytes, project hash, quality profile and editorial frame map. Every completed PNG has an individual hash receipt. Resume reuses only matching pairs; altered, partial or mismatched outputs fail closed. Test a bounded interrupted three-frame prefix before formal film production. Preview frames and Blender starts remain charged to the existing development ceilings.
