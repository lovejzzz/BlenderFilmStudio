# RC6 C21 — same onset, greater severity accepted by composition

Date: 2026-09-02
Status: diagnostic accepted by attempt-99 plus C1 attempt-100

Attempt-99 copied and analyzed all108 C20 cache files. Its base independent
audit passed22/23 and failed only one transcribed C19 receipt self hash. C1
attempt-100 corrects exactly that JSON leaf in memory, binds the complete
retained attempt-99 root, proves the original sole-failure shape and passes
18/18 with zero analyzer, cache copy or Blender work. C1 audit and receipt self
hashes are `b9ee75c2…` and `90cae654…`.

The accepted classification is
`C20_SAME_ONSET_MORE_SEVERE_THAN_C18`. With each run normalized to its own
coherent frame22 state, both radius1.8 and radius1.6 cross velocity support at
frame24, Mesh volume at frame24 and particle support at frame25. Radius1.6 raises
the corresponding maximum expansions from `173.84%`, `51.54%`, `32.38%` to
`769.48%`, `567.15%`, `373.11%`.

The smaller radius therefore amplifies the same impact transition rather than
creating an earlier one. Occupied voxel support is not exact liquid mass, so
this does not prove one Mantaflow internal operation. It does reject further
radius scanning as the next step. Inspect the bound particle-adjustment source
and select one distinct Data-layer degree of freedom before mutation. Rendering
remains forbidden because the physical result is still `FAIL 23/27`.
