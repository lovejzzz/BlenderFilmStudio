# RC6 C29 preregistration — Real-impact particle band width

Date: 2026-09-02
Status: frozen before attempt-108 root creation

C29 returns to exact C18, the materially better high-speed impact baseline, and
changes only Mantaflow `particle_band_width` from `4.0` to its DNA default
`3.0`. The frozen source inspection shows that this narrows continuing
particle preservation/reseeding by one base-grid cell.

The question is two-sided: can a narrower band reduce the late particle/Mesh
support growth, or does it instead remove too much continuing liquid support
and worsen early loss? Later spill, lower velocity support or fewer fragments
cannot pass by themselves. The same 27 physical, provenance, trajectory,
cache, containment, topology, conservation and resource checks from C18 remain
unchanged.

This run does not reinterpret the accepted slow-tip result. In that different
regime, width `4.0` improved temporal loss from `15.443%` to `6.902%`. If width
`3.0` helps high-speed impact, the combined evidence demonstrates regime
sensitivity; it does not authorize one global product default or an adaptive
policy without a separate validation.

Attempt-108 may use exactly one existing RC5 Blender start, one Bullet bake,
one Data bake and one Mesh bake in fresh roots capped at 2 GiB workspace and
64 MiB evidence. Render, scene save, native build, network, film-engine edit,
threshold change, second width value and retained-root mutation remain zero.
Every outcome is retained.
