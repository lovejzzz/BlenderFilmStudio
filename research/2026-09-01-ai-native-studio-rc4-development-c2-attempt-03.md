# RC4 development C2 / attempt-03

Attempt-02 established a valid solver-owned action but failed acceptance. All
four active rigid bodies, derived contact, three target responses, asymmetric
settling, the ten-frame settle window, save/reopen equality and D1/H1
regressions passed. One malformed negative control expected a topology error
but first violated the exact beat schema.

Direct review covered all 48 clip frames and all three stills. The motion was
continuous and physically legible, but the bottle surface read as opaque rough
plastic, the court exposed a black world boundary, the contact clip silently
changed cameras at a timeline marker, and the settled effect shot became a
distant overhead arrangement because it fitted the already-separated ball.
The direct verdict was 6/10 and therefore `FAIL`.

C2 permits only reusable corrections: glass response, a procedural end wall,
exact first-contact framing, response-group-only settled framing, explicit
fixed-camera clip review and the exact-schema negative input. It preserves the
fixture, solver result authority and all thresholds. Attempt-03 uses fresh
roots frozen in
`specs/ai-native-studio-rc4-development-c2-attempt-03.v0.1.json`.
