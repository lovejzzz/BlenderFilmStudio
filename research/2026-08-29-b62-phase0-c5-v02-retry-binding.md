# B62-P0-E1-C5：v0.2 retry 工具与证据绑定

Date: 2026-08-29

Status: **preregistered after D2 PASS, before production-tool change**

D2以3 repetitions/9 of 9动态setter outcomes证明Blender 5.2的因果规则：`IMAGE`拒绝multilayer赋值，`MULTI_LAYER_IMAGE`接受，回到`IMAGE`再次拒绝；final HALF/ZIP exact，zero render。result/receipt self-hash为`6d26de07…/c342ff04…`，独立auditor PASS。

C5授权production generator/renderer只增加先media-type后file-format的顺序，并在calibration report记录mediaType。三个Node工具只允许：绑定C3/C4/C5与D2 PASS、重新验证v0.1 retained failure trees、将正式roots从v0.1换为全新v0.2。其他创作、render、预算、18 gates、16 attacks、verdict和claim boundary全部不变。

顺序冻结为：先提交推送D2+C5且production tools不变；再改五个工具并新tool freeze；fresh clone v0.2 rehearsal；official v0.2 preflight evidence commit；最后只允许一次v0.2 formal attempt。
