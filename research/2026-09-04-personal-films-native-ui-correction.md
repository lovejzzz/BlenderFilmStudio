# Native UI candidate0017: retained visual failure

Native UI source registered, but actual menu labels were corrupted because dynamically returned EnumProperty string tuples did not have persistent Python ownership. Candidate0018 keeps enum items in a module cache and explicitly selects the first shot, then opens a camera-oriented material preview with overlays hidden. This correction is subject to direct UI retest.

The first CUA app lookup used the non-unique display name and launched an older F0.6 engine instead of selecting the already running RC5 process. Its untouched default-cube window was immediately closed; no film load, render, source edit or save was performed there. Count this as one additional non-admitted-by-runner UI start, not zero. Subsequent CUA selection uses the full exact RC5 app path. Candidate0017's isolated process is separately closed and retained. This is an operational exception, not retroactive admission or a scientific verdict.

0016 partial recovery passes: interrupted bytes retained in a unique subfolder, three frames produced across two calls, first completed PNG reused exactly and wrong profile rejected. Total reserved development previews150/192. Final-profile stills completed at1920×818/96 samples; both subjects retain readable detail and controlled highlights. Full-motion/final delivery review still pending.
