# B52-D12.10-C1 independent audit D1 failure

Date: 2026-08-28  
Status: retained audit-tool failure; C1 promotion withheld  
New Blender renders: 0

## Observation

The frozen C1 analyzer completed once with `TEMPORAL_OWNER_SUPPORT_ALIAS_LOCALIZED_C1`, 16/16 internal checks and 30/30 targeted mutations. Its result SHA-256 is `90e8a4d72c0224e4195cd6a52ea193d93211d6dfe368ed7efdd1c3d421d393c8` and its declared analysis hash is `e0f9c8417357ef0d08a75c45ce108d650fe8df22b7a6f3140ea4c3e787ddc719`.

The first independent Node audit did not pass: 7/10 checks and 11/12 independent mutations. Promotion is withheld even though the underlying byte-identity and set-replay checks passed.

## Localized audit defects

1. The Node auditor attempted to reproduce a Python `json.dumps(..., sort_keys=True, separators=(",", ":"))` hash with `JSON.stringify`. Those formats are not cross-language canonical for numeric values: JavaScript serializes values such as parsed `1.0` as `1`, so `RESULT_SELF_HASH` failed without demonstrating a result-byte defect.
2. The `analysis hash` mutation rewrote the hash and then recomputed it before validation. A self-consistent rehash is not an adversarial corruption, so that attack was incorrectly expected to fail.
3. `RESULT_DECISION_AND_TOTALITY` called the same non-portable self-hash check and therefore failed for the same reason, not as a separate C1 measurement failure.

The other seven audit gates passed, including actual byte comparison of all 40 payloads against preserved D1, all 40 declared payload hashes, D1 classification identity, independent set/ratio replay, the formal attack target roster and zero render/model/network calls. Eleven semantic mutations were rejected after their analysis hashes were made internally consistent.

## Correction boundary

The failed audit output remains at `experiments/blender-temporal-owner-support-localization-c1-v0-1/audit.json`; it must not be overwritten. The failed auditor SHA-256 is `d26c0821f3e2dad4195282ce7f8d5805c4d06ffed97d2c83dd305f116095936f`; the failed audit SHA-256 is `3597bddced17737a78fc3e64627fcc7f07c4938aaeb598eda34018aee616d90a`.

A corrected auditor must use a new tool path and write `audit-c1.json`. It may remove the invalid cross-language self-hash gate, bind the exact result file SHA instead, and remove the self-rehash pseudo-attack. It may not change the C1 analyzer, result, payloads, classification or measurements.

## Non-claims

- This failure does not prove the C1 measurements wrong.
- The internally green C1 result is not promoted until a corrected independent audit passes.
- No H1 verdict, threshold, candidate or production claim changes.
