# RC4 development C3 / attempt-04

Attempt-03 passed every machine and independent audit check, fixed the hidden
contact-clip camera cut, closed the environment, and improved the settled-group
composition. Direct review of all 48 frames and three stills reached 8/10.

The remaining defect is isolated to rendered material identity: Eevee's
`DITHERED` surface method produces conspicuous granular alpha coverage on the
otherwise smooth transmissive bottle shells. C3 changes that approved material
family to `BLENDED` and lowers the generic settled-group camera elevation. It
does not alter the fixture, physics, measured beats, solver thresholds or any
outcome transform. Attempt-04 uses the fresh roots frozen in
`specs/ai-native-studio-rc4-development-c3-attempt-04.v0.1.json`.
