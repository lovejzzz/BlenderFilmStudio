# RC4 unstaged physical realism — publication pass

The clean-build accepted product commit
`db662438edfef0a1979d8227c8b58cf8620e2b74` is now the exact public `main` of
`lovejzzz/film-engine`. Its sole parent is the RC3 action-grammar candidate
`5f595fe3aca7118847aec5b572f6d90a377a4352`.

The public update was one ordinary non-force fast-forward. Git remote
inspection, the GitHub commit API and both public raw source files independently
matched the candidate OID, parent and source bytes. No tag, release, binary,
LFS, signing or notarization operation occurred.

The product checkout's remote named `origin` resolves to the retained local
PC9 mirror, not GitHub. That local mirror was first advanced by ordinary
fast-forward from `636f42f2…` to `db662438…`; the separately named `public`
remote was then advanced from `5f595fe3…` to the same candidate. This sequencing
caused no force, rewrite or data loss and is explicitly retained rather than
hidden.

The publication receipt is
`experiments/unstaged-physical-realism/RC4-publication-2026-09-01-attempt-01/receipt.json`
with self hash
`bce3c1d51503d63b27dac0104776a7065e79fce73c2b6d0aeb3fa868e9913d53`.

The public claim remains bounded to the formally accepted M2 Max
procedural-Eevee/Bullet lesson. Publication does not establish optical glass,
fluid simulation, deformation, breakage, sound, cross-platform support,
production readiness, binary distribution or arbitrary photoreal filmmaking.
