# Development iteration 02

Candidate 0002 is retained failed: Bullet contact at frame 75, seven targets responded, last target did not respond and the all-target settled gate never passed. No still was rendered. Change initial domino spacing from .19 m to .16 m, preserving solver ownership, dimensions, release, and completion gates. This is fixture staging, not keyed outcomes. Fresh candidate 0003 only.

The existing tape candidate supplies readable warm/cool separation and machining detail. Final cinematic acceptance is pending all shots and motion. Add Metal selection when the installed Cycles runtime exposes a Metal device; retain CPU fallback and record real runtime logs. No clean build or new charge.

The AI director adapter uses already authenticated ChatGPT Codex CLI, returns one bounded typed proposal and rejects tool activity. It has not yet been executed. Official behavior checked at https://learn.chatgpt.com/docs/non-interactive-mode (2026-09-04). UI applies only revision-checked data, not model code.
