# RC6 C31 preregistration — Evidence scope before adaptive recipes

Date: 2026-09-02

C29/C30 exposed opposite bandwidth responses under slow-tip and impact motion.
Both impact candidates still fail the complete physical gate. C31 therefore
inventories measurable regime features and freezes a non-executable
evidence-scope policy, not a speed-to-bandwidth lookup.

The frozen spec binds twelve source/evidence files and the current local pure
policy source commit `031793e09e60532b7406009ad47c06afef77876b`. This is a local source
identity; C31 makes no fresh public-remote identity claim. One system-Python
producer and one independent auditor may read those files and create only
attempt-110 research evidence (maximum 4 MiB). No work root, Blender, bake,
cache copy, product import, product edit, render or validation-network operation
is allowed.

The producer derives slow-tip surface travel over its actual accepted 1–24
window, not the full 120-frame motor screen. It compares the two 1–36 impact
results, records the complete failed-check vectors, reads the product's tier
table by AST, and records common configuration differences. The independent
auditor recomputes these fields without importing either the producer or product.

A one-voxel sampling boundary is a dimensional description, not a fitted
stability threshold. Current settings and experiment IDs are evidence bindings,
not branches in future product code. A new geometry/topology/window remains
uncalibrated even if its speed resembles a passing case. Existing product
PREVIEW ceilings and all physical gates remain unchanged.

The spec also freezes a future rectangular-vessel, linear-motor holdout with
zero authored outcome keys. It is distinct from the cylindrical rotating cup
and basketball strike. Its physical result is UNKNOWN, its execution count here
is zero, and it needs a separate bounded static/Bullet/liquid protocol before
execution. It cannot establish a high-speed recipe merely by passing.

Acceptance is 10/10 inventory checks and 20/20 independent checks, exact retained
input bytes, and zero forbidden operations. C31 PASS means the inventory and
design are consistent; it cannot mean impact physics or filmmaking is solved.
