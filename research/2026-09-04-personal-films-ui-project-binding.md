# UI-only project binding correction, product0.1.1

While the two formal renders use their already frozen source copies, source review finds that UI proposal application checked only a revision number. Two different films can share revision1. Bind both the asynchronous director return and the Apply action to the exact input document hash. Reject a response if the user opens another film or changes direction before it arrives. This does not change the frozen asset compiler, cached-frame evaluator, renderer, final inputs or running processes.

Candidate0031 performs two actual registered-UI checks without model calls/renders: enqueue a proposal from filmA while filmB with the same revision is current (must not install pending data), then attempt to apply that old proposal directly (must reject with filmB unchanged). Product version becomes0.1.1 for this UI correction. The final films retain exact formal renderer provenance0.1.0; no claim that their immutable source copies were edited.
