# RC6 C20 C1 preregistration — fresh-root restart adapter

Date: 2026-09-02
Status: preregistered before attempt-94 root creation

Attempt-93 stopped only because the owner requested a Codex restart. It retained
11 partial Data frames, no Mesh and no scientific verdict. It is immutable and
must never be resumed, repaired or used as measurement input.

C1 repeats the already frozen C20 question once in unique attempt-94 roots. It
changes no scene or physical value: simulation particle radius remains1.6 on
the exact C18 fractions-threshold0.10/CFL2/timesteps2/8 baseline; trajectory,
geometry, Mesh settings, all27 gates and all resource ceilings remain exact.
The unchanged C20 scene tool is reused byte-for-byte. Versioned runner/auditor
adapters change only fresh-root/provenance bindings and independently verify the
retained attempt-93 evidence and partial workspace snapshot.

The latest general host preflight observes155 GiB free and therefore denies a
new60 GiB clean build. C1 performs no build and admits only the existing2 GiB
projected-write experiment while preserving the100 GiB reserve. One Blender,
one Bullet/Data/Mesh attempt and zero render/save/network/engine writes are the
unchanged ceilings. Every completed physical outcome is retained; another
interruption or harness failure is retained without in-place correction.
