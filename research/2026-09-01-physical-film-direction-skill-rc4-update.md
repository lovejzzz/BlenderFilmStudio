# Physical Film Direction skill — RC4 update

RC4 attempt-02 added two observed, reusable lessons to the local
`physical-film-direction` skill:

- a background fixed-camera render can still be replaced by timeline camera
  markers even after assigning the camera after `frame_set`; neutralize markers
  only in the unsaved review process, record them, and inspect both sides of
  each former marker;
- a settled response-group shot should fit the response group rather than a
  distant initiator, and strong bump on a transmissive shell can make glass
  read as opaque stippled plastic in Eevee.

The skill entrypoint remains unchanged with SHA-256
`93e8ad93724c6e9316a67385b3e5428caefc551273bb1d85553e0e654f1ed4d3`.
The updated validated-patterns reference SHA-256 is
`9d115a00edcd51d16dab83fa159f5ef440030c9dd5eb5a5295ca901bbc9a82d7`.
The skill-creator validator returned `Skill is valid!`.
