# RC6 C35 preregistration — native-phi operation order

Date: 2026-09-03

C35 is a zero-Blender, zero-mutation source/evidence audit. It binds exact RC5
Mantaflow code to accepted C34 native fields and corrects the narrative onset
context before selecting another experiment.

It must prove the order of particle advection, obstacle push, beginning-frame
phi copy, current-phi advection, particle-levelset construction, current-phi
shrink/join, pressure, particle resampling and APIC particle update. It must
also bind the concrete kill/reseed rules in `adjustNumber`, the fixed cell-unit
support widths, the APIC branch's non-use of `flip_ratio`, and independently
recompute the C34/C29/C30 curves.

Selection is frozen as a diagnostic layer, not an accepted recipe. If all
bindings pass, C36 may change only `resolution_max 96→128` in an otherwise
exact C34 Data-only native-field run. Resolution 128 is already the product's
frozen REVIEW tier. This tests whether the numerical loss converges when the
fixed-cell level-set and particle kernels change world scale; it does not tune
a physical scalar, admit the 36-frame request through the product API, or
authorize Mesh/render. If the source or metrics fail to bind, no C36 is
selected.

The retained impact remains physical FAIL25/27. C35 cannot claim exact mass,
same-frame Data/Mesh onset, a responsible solver operation, repaired physics,
visual quality or permission to adjust lighting.
