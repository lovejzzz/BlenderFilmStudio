# PC.2 zero-render action tool freeze

The exact PC.2 builder, reopen semantic auditor, two-start runner and independent final auditor are frozen before formal root creation. The builder adds only the preregistered rig/IK/socket, four-gimbal and two-shoulder-fin curves, then saves one derivative. The reopen process independently locates the actual F-curves and verifies every required frame and amplitude. Geometry, materials, camera/light sentinels and shot markers must remain exact; any media artifact fails the run.
