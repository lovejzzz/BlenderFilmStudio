# Reusable shot creation

Add a native Add coverage shot action: split the selected shot into two chronological camera angles while preserving total duration, source range and the physical world. The new shot has a stable unused ID and can receive the same director edits. Undo removes the added product camera and restores the original shot document. This avoids an empty duplicate beyond the film's simulated range.

Candidate0023 exercises the actual registered operator on copied0022 kinetic world, verifies one extra shot and unchanged actual mesh/poses, then undoes and verifies camera inventory plus physical state. Zero renders or new bake. This completes the product's shot-creation requirement before the formal code freeze.
