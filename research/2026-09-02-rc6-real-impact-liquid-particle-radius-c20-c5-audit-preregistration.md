# RC6 C20 C5 preregistration — exact historical audit views

Date: 2026-09-02
Status: preregistered before attempt-98 evidence-root creation

C4 proved the shared execution namespace and the frozen `2e-8 m` centroid
replay tolerance. It retained two failures caused only by reconstructing the
past from a later audit-only context.

C5 changes exactly two views: attempt-94's expected Blender argv uses the
retained attempt-94 evidence root, and the pre-audit manifest replay excludes
the independent audit plus its stdout/stderr logs that were created after that
snapshot. C4's shared namespace, the `2e-8` centroid tolerance, both `1e-8`
volume tolerances, all27 physical checks, retained bytes and hashes, claim and
zero-Blender ceilings remain unchanged. Attempts93/94/96/97 stay immutable;
attempt-95 stays absent. The only allowed output is fresh attempt-98.
