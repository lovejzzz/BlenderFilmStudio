# RC6 slow-tip Bullet screen preregistration

Date: 2026-09-02  
Status: preregistered before attempt-47 root creation

The accepted Final static liquid closes the container-at-rest prerequisite. It
does not justify spending another multi-hour Mantaflow run on an unmeasured
moving collider. This gate therefore isolates the next causal question: choose
the slowest of four frozen visible-striker drives that still lets Bullet own the
ball contact and the tumbler response.

The tumbler and basketball keep zero animation channels. Only the already
visible kinematic striker expresses intention. Contact, 5-degree response,
45-degree response, peak tilt, swept cup bounds and maximum cup-surface motion
are derived from the evaluated Bullet cache. The moving-effector subframe count
is `ceil(maximum cup surface displacement per frame / Preview base voxel)`; it
is not carried forward from the failed impact pipeline's fixed value of four.

Attempt-47 permits four Bullet-only Blender starts and four narrow rigid-body
cache bakes. It forbids Mantaflow Data/Mesh bakes, renders, blend saves, native
builds, network calls and engine writes. The selection is the greatest frozen
drive-end frame that passes every check. A PASS selects an input for a new
slow-tip liquid protocol only; it is not a liquid or cinematic result.

Frozen machine contract:
`specs/ai-native-studio-rc6-slow-tip-bullet-screen.v0.55.json`.
