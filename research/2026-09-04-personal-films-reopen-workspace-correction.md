# Native workspace reopen correction, product0.1.2

Delivery GUI start1 opened the exact packaged tape project. A real Chinese Codex director request returned camera distance4.0 while preserving42mm; Apply visibly changed the composition to revision2. Save produced `~/Movies/Personal Film Studio/last-signal/last-signal-r0002-dd3f142f.blend` and its typed document, preserving the packaged revision1 bytes.

Opening packaged Little Gravity in that same process exposed a real usability failure: Blender restored the input file's solid viewport, overlays and closed sidebar. The studio's material-preview setup ran only at initial module registration. The project loaded, but the promised working view did not follow it. Retain this observed failure rather than describing the complete reopen experience as accepted.

Correct only workspace presentation: one persistent load handler reapplies the existing material-preview/camera/sidebar setup to personal-film documents. Preserve an existing valid shot selection. Use the runtime's existing Expert Mode for these documents so legacy research-header fixture identifiers and unrelated contract controls are not presented as this film's controls. No native source/binary, scene compiler, physics, renderer or running final copy changes.

The second and last GUI start under delivery admission3539df25 will use the packaged Little Gravity launcher, then reopen the actual saved tape revision and return to the kinetic project. Inspect material view, panel, title/revision and shot selection. No new render, simulation, build or model request is needed. Product version becomes0.1.2; formal movies retain their exact frozen0.1.0 pipeline provenance.
