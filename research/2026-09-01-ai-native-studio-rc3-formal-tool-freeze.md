# RC3 physics-native action formal tool freeze

Date: 2026-09-01

The RC3 clean-build formal runner and independent auditor are frozen before
creation of either formal root. The run admits only product candidate
`5f595fe3aca7118847aec5b572f6d90a377a4352`, its sole parent
`636f42f28f781f3e858fd5b6bf641910a549c91b`, the corrected D1 v0.3 and H1
v0.2 graphs, and the already accepted RC3 development and direct-visual roots.

The formal run permits one local no-checkout clone, one zero-network LFS
checkout from retained immutable objects, one clean native arm64 build, six
product starts, two scene mutations and saves, two reopens, sixteen negative
controls, six stills and ninety-six contact-clip frames. It also verifies the
installed RC1, RC2 and causal fallback routes without an additional product
start.

Formal visual acceptance may reuse the direct 10/10 review only when every
formal still, contact sheet and complete clip is byte-identical. Any difference
forces new direct inspection. No engine remote write, release, distribution,
signing, notarization, LFS transfer or authored outcome/event pose is allowed.

The machine freeze is
`specs/ai-native-studio-rc3-physics-action-formal-tool-freeze.v0.1.json` with
self hash
`ef88e98f256eae40fd1b07f4f96553d01cb8dfedaeb7bc24c64bba1195f59e04`.
