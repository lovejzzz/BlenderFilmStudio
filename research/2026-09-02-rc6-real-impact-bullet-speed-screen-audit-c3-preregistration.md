# RC6 real-impact Bullet speed screen audit-only C3 preregistration

Date: 2026-09-02

C3 performs no Blender start and no physics. It binds the complete immutable
18-file attempt-73 root (`136000` bytes, manifest hash `8390c039…`), its
self-hashed physical FAIL receipt, and its retained 22/23 audit. The sole
correction compares Vector-backed Blender float32 configuration values against
the exact decimal protocol with absolute tolerance `1e-6`.

All physical results, thresholds and verdicts remain unchanged. C3 must write
only a corrected audit and manifest in the fresh attempt-74 evidence root, then
prove the retained attempt-73 manifest is byte-identical. Blender, Bullet,
liquid, render, save and network counts are all frozen at zero.

After audit closure, the physical evidence selects one new question:
`driveEndFrame=9`. C3 does not authorize or run that question.
