# RC4 development C4 / attempt-05

Attempt-04 passed its 18/18 independent machine audit and preserved the full
solver-owned action. The lower settled composition improved, and blended alpha
removed the coarse dithering. Full-resolution pixels still showed unacceptable
Eevee transmission noise on the bottle shells, so direct review remained 8/10.

C4 changes one material leaf in the approved household-glass look: transmission
weight becomes zero and alpha becomes 0.28. The visible shell keeps a smooth
coated highlight, bounded tint and transparent silhouette, while the separate
internal fill remains visible. This is a stable film-look approximation, not a
claim of optical refraction. Physics, fixture, cameras and thresholds remain
unchanged. Attempt-05 uses fresh roots frozen in
`specs/ai-native-studio-rc4-development-c4-attempt-05.v0.1.json`.
