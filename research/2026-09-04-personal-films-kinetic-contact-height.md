# Source and trajectory finding: contact below the center of mass

0004 repeats the .16 m staging but writes all600 poses before its retained completion failure. The first five targets translate right while rotating backward (negative Y), and the later pieces remain upright. The .075 m ball contacts below the .145 m target center, pushing the base instead of initiating a forward toppling chain. Further spacing scans are not justified.

0006 changes the ball's initial geometric radius to .16 m and derives its initial height from the unchanged ramp height plus radius. The event detector derives its reach from the actual sphere dimensions. Target geometry, spacing, solver, release and completion gates remain unchanged. No response pose is authored. This is a hypothesis to test, not an accepted causal/result claim.
