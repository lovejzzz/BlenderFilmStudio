# PC.2 action-complexity increment preregistration

PC.2 starts from the accepted PC.1 derived scene and targets the owner-observed “动作太简单” weakness without changing the protected lighting, camera language or modeling result. Four causal channels are frozen: approach weight transfer, contact reach/lock, activation recoil with core mechanism, and reflection/breath/shoulder settle. Ten authorized non-camera targets are available; at least six must animate, all four phases must pass explicit amplitude floors, and camera/light sentinels plus PC.1 geometry/material identity must remain exact.

The formal attempt is zero-render: one build/save start and one reopen/audit start. PC.3, not PC.2, will render the integrated comparison slice. This keeps semantic action acceptance separate from later image/video judgment and prevents lighting or camera changes from hiding weak movement.
