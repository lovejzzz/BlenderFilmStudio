# PC.2 C1 structural protected-state comparison freeze

Attempt-01 remains sealed. C1 binds the accepted PC.1 `build.json` by exact file SHA and replaces only the cross-language numeric hash comparison with direct `protectedStateAfter` structure equality in the builder, reopen auditor and final auditor. Versioned wrappers verify the exact frozen base-tool hashes and exact single patch sites before execution. All action phases, amplitudes, targets, geometry/material checks, camera/light values, shot state and ceilings are unchanged. Fresh attempt-02 roots were absent at freeze.
