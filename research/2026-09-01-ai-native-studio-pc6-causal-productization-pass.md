# PC6：真实物理合同已进入产品，电影级真实感仍未通过

PC6 的结论分成两个互不混淆的层次。

第一层已经通过：真实的 Film Studio Engine 原生构建现在提供 `Physics`
入口，可以检查并执行受限的声明式 CausalSceneSpec。场景合同只能选择五类受控工厂
和有限物理参数，不能携带 Python、shell、网络、任意文件路径或最终姿态权限。球在
释放前只获得初始条件；释放后以及所有目标物体的位姿都由 Blender/Bullet 求解。
摄像机读取求解帧的语义包围盒，而不是消费某次作品的最终坐标。

第二层没有通过：三张原始分辨率画面能够清楚表达起势、接触和结果，但仍像精致的
玩具演示。IMPACT 选择了首次响应帧，尚未展示最强的连锁倾倒；四个目标最终几乎
全部水平；画面缺少运动模糊、接触形变和二次物理。不能用材质或特效掩盖这些问题，
更不能手工摆最终姿态。

验证结果：

- 产品源码：`5f3b981a6d84fd49d2eaafe35645456bf4d669e5`，相对基线只有三个路径、
  702 additions、0 deletions。
- attempt-05：一次 clean native arm64 build，九个负向控制，三次产品启动，一次
  合法场景 mutation，三张 review render，一次 save/reopen。
- 物理：四个目标在 28/31/31/30 帧响应，最终倾角均约 90°；目标位姿 keyframes=0，
  release 后 actor 位姿 keyframes=0，reopen 倾角差全部为 0.0°。
- 独立审计：基础 `21/21 PASS`，C5 绑定审计 `11/11 PASS`。
- 最终 acceptance self hash：
  `cd334acd197029308a2d42f6159acfd05f0c465e2e115e2e8e21b8e2a20e3777`。

PC7 的唯一正确方向是继续使用真实求解结果：从 evaluated motion 选择传播最强的
撞击时刻，对初始间距、朝向、摩擦和回弹加入可复现的受控微差，并加入受限的二次
物理与快门表现。任何为了好看而直接写入倒地姿态的做法仍然禁止。
