# RC6 C24 C1 preregistration — one-token analyzer correction

Attempt-102 is retained unchanged at harness `FAIL 7/8`. Its sole false check
compared actual C23 verdict `FAIL_REAL_IMPACT_LIQUID_PARTICLE_MAXIMUM_C23`
against a stale adapted expectation ending in `PARTICLE_RADIUS_C23`.

C1 changes exactly that expected token in a versioned analyzer. It does not
change any VDB or Mesh input, frame measurement, onset threshold, amplitude
rule, correlation calculation, classification branch, runtime identity,
resource ceiling or prohibited-operation count.

Fresh attempt-103 must:

- copy the exact immutable 108-file C23 cache;
- bind the exact retained attempt-102 work/evidence manifests before and after;
- repeat all 36 frozen measurements with one analyzer;
- run the independent auditor separately with one additional engine-Python
  start;
- create no Blender process, bake, render, save, network call or retained-root
  write.

Run once and retain every outcome. A passing C1 audit closes the C24 diagnostic
only; it cannot change C23's physical FAIL or authorize rendering.
