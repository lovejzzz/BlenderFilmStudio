# Create a personal film from an editable starting world

The delivered controls currently save revisions of the supplied film but offer no direct way to name a new personal film. Add one bounded user-facing operation: **New film from this…**, with a title dialog, fresh stable project ID, revision1, independent history and a unique saved project. Preserve all existing assets, shots, animation and baked physics. Clear any old pending director suggestion and old render pointer so they cannot cross into the new film. Invalid/empty titles reject before changes; a failed save restores the current in-memory document/history. No compiler rebuild occurs.

This is a small completion of the current personal-authoring workflow under the standing charter. Product0.1.3 adds this native action and Chinese title-card font coverage/width fitting. Existing final films continue with their frozen0.1.0 copies; no final frame, encoder, input or running worker is changed.

Use pure document checks for fresh identity, independent copy, unchanged protected world and invalid title rejection. Admit exactly one additional native GUI start, capped at10minutes, under fresh `experiments/personal-films/PF-NEW-PROJECT-2026-09-04-attempt-01`. Open a packaged starter, create a named film, verify its independent file/document and unchanged source hash, then reopen the new film in the same session. Render/bake/build/model calls are zero. A local transparent title PNG may be made with Pillow to verify Chinese glyph coverage and fit; it is not a Blender render. This stays within the existing2GiB delivery/review allowance and100GiB free reserve. Retain any failure and do not repeat this GUI admission.
