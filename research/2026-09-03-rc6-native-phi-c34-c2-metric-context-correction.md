# RC6 C34 C2 metric-context correction

Date: 2026-09-03

The accepted C34 evidence and its strong common-field-equivalence verdict do
not change. One narrative sentence compared two differently based 15% loss
crossings as though they were the same measure.

The exact corrected values are:

- current native phi, relative to its own frame 1: first `≤-15%` at frame 31;
- retained C29 Mesh, relative to its own frame 1: first `≤-15%` at frame 25;
- retained C30's reported Mesh loss onset: frame 31 relative to its coherent
  frame-22 baseline;
- current native phi, relative to frame 22: first `≤-15%` at frame 33.

Therefore C34 does **not** establish same-frame Data/Mesh onset. The accepted
facts remain: current phi ends `-27.1968%` relative to frame 1, Mesh ends
`-39.1308%`, their 36-frame correlation is `r=0.89898`, and all common particle
and velocity fields are exact. This continues to reject a purely Mesh-only
account of the final loss, while preserving inconclusive transition ordering,
no exact-mass claim and no individual solver-operation attribution.

C35 independently recomputes these values from the retained evidence before
using them in its read-only source-order decision.

