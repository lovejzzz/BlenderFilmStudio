# PC4-VX1 typed executor tool freeze

状态：首次 typed derivative mutation 前冻结。

冻结输入包括 accepted VisualImprovementPlan、VisualReviewPacket、execution context、两个受信 Python adapter、fresh-root runner、独立 Node auditor、15 项静态合同测试以及共享 canonical hash 模块。源 `.blend` 与 admitted arm64 binary 由 context 逐字节绑定。

几何实现只允许从 operation type/preset、plan target entity、shot camera 与 evaluated bounding box 推导。静态检查确认 executor 中没有任何 plan target 名称特例。最多两次 Blender start、三张 PNG、一次 save；直接视觉复核前不得渲染完整序列。

