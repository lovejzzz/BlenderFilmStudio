# B50 focus-intent delayed-disclosure package result

Date: 2026-08-27  
Preregistration commit: `04365d44c16c9be65582184d9ff4d697e5d6e2f6`  
Tool freeze: `e6f8540738e02e0562c8aac4b19edc3a79d834f6`  
Spec SHA-256: `244d6cc3839bd2923fd85e5069d44d30938bc4b2e8c0a805505c63e2cb789f20`

## Verdict

`PACKAGE_READY_HUMAN_PENDING`. The two real Blender conditions, eighteen balanced blinded sessions, response chain, delayed-disclosure commitments and interface pilot are ready. There are zero valid human responses; no focus-intent or cinematic-preference outcome exists.

## Measured facts

The first preflight rejected execution before any container because the frozen 2 GiB write projection would have left `106,700,288,000` bytes, below the 100 GiB reserve. Only the exact 1.62 GB reconstructible `/Users/tianxing/.cache/codex-runtimes` cache was removed under the existing cache-cleanup authorization. The unchanged gate then admitted the run.

Two fresh Linux/amd64 Blender 5.2 Cycles CPU workers rendered frame 23 of the byte-bound INTERIOR scene at 960×540, 128 raw samples, four threads, f/4, 70 mm and centered 0.5-frame motion blur. The original cell retained the 3.2 m numeric focus and null focus object; the intervention changed only `camera.data.dof.focus_object` to the semantic `PROP_CHAIR` root. Fresh-worker walls were 670.342 and 667.678 seconds.

Both scene-linear multipart EXRs reopened with the exact seven production subimages and finite arrays. Both ACES 2 SDR review PNGs reopened as 960×540 RGB. The conditions differed in 1,553,929 scene-linear RGB values with maximum absolute error 0.421772, and in 373,958 encoded PNG channel values with maximum absolute difference 112. These are machine differences, not human judgments.

The package contains eighteen private observer sessions with nine orders of each class. Every folder contains only `IMAGE-A.png`, `IMAGE-B.png` and the anonymous interface. Condition labels, source paths, repository links, mappings and unblinding material were absent. A private sensitive registry and whole-package opening were reduced to salted public commitments.

The package evaluator passed all 21 frozen attacks. The independent auditor reopened the media, recomputed bindings, mappings and commitments, checked 29 facts and scanned 3,616 tracked/public-build files against the private registry with zero matches. No B50 container remained.

A real in-app-browser pilot loaded both images at native and CSS size 960×540, switched A→B correctly, found no document leak and no console error, and neither submitted the form nor downloaded a response. It is interface evidence only. Human count remains 0/18.

## Disclosure boundary

Condition-labelled output hashes, PNG/EXR filenames, observer-visible image identities, mappings, salts and responses remain in ignored private storage until collection closes. The public repository receives only the preregistered design, tool source, salted commitments, aggregate package facts and the explicit pending status.

The collection is not yet open. After the exact source and site build are committed and published, a final private registry scan must pass on that exact public state. Response acceptance then requires a new same-state scan every time.

## Non-claims

This package is not evidence that either focus is preferable. It does not establish general cinematic quality, photorealism, composition optimality, acting, narrative, identity consistency, focus-pull quality, full-shot stability, 2K/4K adequacy, GPU throughput, cloud cost or delivery-master quality. Even a later formal human result applies only to this frozen scene, pair, observers and recorded remote conditions.

Artifacts: `specs/focus-intent-human-review-spec.v0.1.json`, `experiments/focus-intent-human-review-v0-1/precollection-commitment.json`, `experiments/focus-intent-human-review-v0-1/package-status.json`, `research/2026-08-27-b50-focus-intent-human-review-protocol.md` and private ignored `experiments/focus-intent-human-review-v0-1/work/`.
