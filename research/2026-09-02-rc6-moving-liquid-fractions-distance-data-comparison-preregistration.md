# RC6 moving-liquid fractions-distance Data comparison preregistration

Date: 2026-09-02

Status: preregistered before attempt-69 root creation

Attempt-68 changed only fractional-obstacle distance 0.5→0.25 and reduced
temporal Mesh loss to a 15.44% near-pass with zero cup intrusion. Attempt-69
tests whether that response begins in Data support before Mesh reconstruction.

The frozen run copies the exact immutable attempt-68 cache into one fresh root
and binds it as `fractions-distance-0p25` against attempt-66's analysis of the
attempt-65 `fractions-distance-0p5` Data cache. It reuses the exact generic
copied-VDB analyzer and binds all 24 frames, result/audit hashes, surface
distance 2.0, one effector subframe, exact VDB roster and voxel scale.

No Blender, bake, render, save, network or retained-root write is allowed.
Occupied voxels remain a spatial-support proxy, not exact mass. No second
fractional distance, Mesh tuning, impact or render follows this diagnostic.
