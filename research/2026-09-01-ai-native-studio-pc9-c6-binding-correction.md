# PC9 C6 — binding correction

C2 recorded an incorrect file SHA for its C1 parent. C1's content and canonical `specHash` remain unchanged, and historical files are not edited. C6 records the actual C1 file SHA and requires the formal freeze to bind every contract's current bytes directly.

No product, fixture, threshold, development result or formal-root change is authorized.
